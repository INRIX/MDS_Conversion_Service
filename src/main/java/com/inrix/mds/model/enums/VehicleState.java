package com.inrix.mds.model.enums;

public enum VehicleState {
    removed,
    available,
    non_operational,
    reserved,
    on_trip,
    stopped,
    non_contactable,
    missing,
    elsewhere
}
